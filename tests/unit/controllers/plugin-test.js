import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('controller:plugin', 'PluginController', {
  needs: ['controller:application']
});

// Replace this with your real tests.
test('it exists', function() {
  var controller = this.subject();
  ok(controller);
});
